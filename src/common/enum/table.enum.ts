export enum TableItemTypeEnum {
  RECORD = 'record',
  HEADER = 'header',
}

export enum ColumnTypeEnum {
    STRING = "string",
    ENUM = "enum",
    NUMBER = "number",
    DATETIME = "datetime",
    BOOLEAN = "boolean",
    ARRAY = "array",
    OBJECT = "object",
}